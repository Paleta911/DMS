# TESTS_DMS Frontend

Ejecutar:

```bash
npx vitest -c TESTS_DMS/frontend/vitest.config.ts run
```

Estas pruebas viven fuera de `frontend/src` para cumplir con el requerimiento de separar la batería generada en `TESTS_DMS`.
