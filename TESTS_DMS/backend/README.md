# TESTS_DMS Backend

Ejecutar:

```bash
npx jest -c TESTS_DMS/backend/jest.config.cjs
```

Estas pruebas viven fuera de `backend/src` para cumplir con el requerimiento de separar la batería generada en `TESTS_DMS`.
